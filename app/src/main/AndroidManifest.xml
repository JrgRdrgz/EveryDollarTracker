<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    android:name=".EveryDollarTracker"
    package="com.example.everydollartracker">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EveryDollarTracker">
        <activity
            android:name=".note_each"
            android:exported="true" />
        <activity
            android:name=".edit_profile"
            android:exported="true" />
        <activity
            android:name=".note_view"
            android:exported="true" />
        <activity
            android:name=".note_detail"
            android:exported="true" />
        <activity
            android:name=".Settings"
            android:exported="true" />
        <activity android:name=".Income" />
        <activity android:name=".Expense" />
        <activity android:name=".App_Page" />
        <activity android:name=".register" />
        <activity android:name=".login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>